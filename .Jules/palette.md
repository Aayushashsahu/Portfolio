## 2024-05-24 - Form a11y & ARIA labels
**Learning:** Explicit label-to-input bindings using 'for' and 'id' are required in this app's .fg component architecture for screen reader accessibility.
**Action:** Always ensure explicit 'for' and 'id' bindings are implemented between labels and inputs in custom form groups.