## 2026-06-29 - Missing explicit form labels
**Learning:** This app uses a custom `.fg` form group pattern with generic `<label class="flabel">`. Screen readers and click-to-focus need explicit `for` and `id` bindings.
**Action:** Always verify if custom form abstractions maintain native accessibility traits.
