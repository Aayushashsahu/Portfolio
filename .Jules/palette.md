## 2024-10-24 - Form Label Association
**Learning:** In the `.fg` form group pattern, missing explicit `for` and `id` bindings makes forms inaccessible to screen readers and reduces the clickable area for users.
**Action:** Always ensure explicit `for` and `id` bindings are implemented between labels and inputs, especially in custom UI patterns.
