## 2026-05-24 - Explicit Form Label Bindings
**Learning:** Implicit label wrapping isn't always enough for optimal screen reader support, especially when custom styles or components break standard document flow.
**Action:** Always ensure explicit `for` and `id` bindings are implemented between labels and inputs, even within styled form groups.
