## 2026-06-28 - Form Accessibility Bindings
**Learning:** The custom `.fg` form group pattern requires explicit `for` and `id` bindings between labels and inputs to ensure screen reader accessibility and improve usability by expanding the click target to the label.
**Action:** Add `for` and `id` attributes to all form labels and inputs within the contact form to ensure correct semantic association.
