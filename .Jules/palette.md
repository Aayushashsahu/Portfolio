## 2026-06-10 - Explicit Label Bindings
**Learning:** Custom form group patterns (like `.fg` with `.flabel` and `.finput`) often omit native HTML bindings. Screen readers require explicit `for` and `id` relationships to properly associate labels with inputs.
**Action:** Always ensure explicit `for` and `id` bindings are implemented between labels and inputs in custom form components.
