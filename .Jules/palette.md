## 2024-10-24 - Form Label Bindings
**Learning:** The custom `.fg` form group pattern requires explicit `for` and `id` bindings between `<label class="flabel">` and `<input class="finput">` (or `<textarea>`) to ensure screen readers correctly associate labels with their respective inputs.
**Action:** Always implement explicit form bindings for all form labels and inputs within the application.
