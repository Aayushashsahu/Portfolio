## 2024-10-24 - Form Label Accessibility
**Learning:** Custom `.fg` form groups lacked explicit `for` and `id` bindings between labels and inputs, preventing clicking labels from focusing the inputs.
**Action:** Add explicit `for` and `id` attributes to all form labels and inputs.
