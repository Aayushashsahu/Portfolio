## 2024-05-24 - Form Group Accessibility Binding
**Learning:** Custom form group patterns (like `.fg` with `.flabel` and `.finput`) often lack native `for` and `id` bindings, preventing users from clicking labels to focus inputs and causing screen reader accessibility issues.
**Action:** Always ensure explicit `for` and `id` bindings are implemented between labels and inputs in custom form components.
