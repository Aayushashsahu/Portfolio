## 2024-05-18 - Missing Explicit Form Labels
**Learning:** Custom form group patterns (like '.fg') often rely on visual layout to associate labels with inputs, breaking accessibility and click-to-focus behavior.
**Action:** Always ensure explicit 'for' and 'id' bindings are implemented between labels and inputs, even when visually grouped.
