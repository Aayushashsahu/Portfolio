## 2024-05-18 - Form Accessibility Binding
**Learning:** Custom form group patterns (like `.fg` with `<label>` and `<input>`) often miss explicit `for` and `id` bindings, which breaks screen reader association and click-to-focus behavior.
**Action:** Always verify and implement explicit `for` and `id` bindings in custom form components to ensure robust accessibility.
